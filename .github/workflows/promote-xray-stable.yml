name: Promote Stable Xray Image

on:
  workflow_dispatch:
  schedule:
    - cron: '17 4 * * *'

concurrency:
  group: xray-image-alias-update
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: taoziyoyo2566/xray_docker
    permissions:
      contents: read

    steps:
      - name: Checkout codebase
        uses: actions/checkout@v4

      - name: Resolve upstream stable and registry digests
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          version="$(curl --fail --silent --show-error --location \
            -H 'Accept: application/vnd.github+json' \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'X-GitHub-Api-Version: 2026-03-10' \
            'https://api.github.com/repos/XTLS/Xray-core/releases/latest' | jq -r '.tag_name')"
          version_digest="$(curl --fail --silent --show-error --location \
            "https://hub.docker.com/v2/repositories/${IMAGE_NAME}/tags/${version}" | jq -r '.digest')"
          latest_digest="$(curl --silent --show-error --location \
            "https://hub.docker.com/v2/repositories/${IMAGE_NAME}/tags/latest" | jq -r '.digest // empty')"
          stable_digest="$(curl --silent --show-error --location \
            "https://hub.docker.com/v2/repositories/${IMAGE_NAME}/tags/stable" | jq -r '.digest // empty')"

          if [[ ! "${version}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ || ! "${version_digest}" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo "::error::Upstream stable ${version} does not have a valid image tag"
            exit 1
          fi

          promote=false
          if [[ "${version_digest}" != "${latest_digest}" || "${version_digest}" != "${stable_digest}" ]]; then
            promote=true
          fi

          {
            echo "version=${version}"
            echo "digest=${version_digest}"
            echo "image_ref=${IMAGE_NAME}@${version_digest}"
            echo "previous_ref=${IMAGE_NAME}@${latest_digest}"
            echo "promote=${promote}"
          } >> "${GITHUB_OUTPUT}"

          if [[ "${promote}" == 'true' && ! "${latest_digest}" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo "::error::Current latest tag does not provide a rollback digest"
            exit 1
          fi

      - name: Set up QEMU
        if: steps.release.outputs.promote == 'true'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: steps.release.outputs.promote == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: steps.release.outputs.promote == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Verify promotion target
        if: steps.release.outputs.promote == 'true'
        id: verify
        env:
          IMAGE_REF: ${{ steps.release.outputs.image_ref }}
          VERSION: ${{ steps.release.outputs.version }}
        shell: bash
        run: bash docker-build/verify-image.sh "${IMAGE_REF}" "${VERSION}"

      - name: Verify rollback image
        if: >-
          steps.release.outputs.promote == 'true' &&
          steps.release.outputs.previous_ref != steps.release.outputs.image_ref
        id: rollback_verify
        env:
          IMAGE_REF: ${{ steps.release.outputs.previous_ref }}
        shell: bash
        run: bash docker-build/verify-image.sh "${IMAGE_REF}"

      - name: Promote stable and latest aliases
        if: steps.release.outputs.promote == 'true'
        env:
          DIGEST: ${{ steps.release.outputs.digest }}
        run: |
          docker buildx imagetools create \
            --tag "${IMAGE_NAME}:stable" \
            --tag "${IMAGE_NAME}:latest" \
            "${IMAGE_NAME}@${DIGEST}"

      - name: Report promotion
        env:
          DIGEST: ${{ steps.release.outputs.digest }}
          PROMOTED: ${{ steps.release.outputs.promote }}
          VERSION: ${{ steps.release.outputs.version }}
          PREVIOUS_REF: ${{ steps.release.outputs.previous_ref }}
          PREVIOUS_VERSION: ${{ steps.rollback_verify.outputs.version }}
        run: |
          {
            echo '## Stable Xray promotion'
            echo
            echo "- Version: \`${VERSION}\`"
            echo "- Digest: \`${DIGEST}\`"
            echo "- Aliases changed: \`${PROMOTED}\`"
            if [[ -n "${PREVIOUS_VERSION}" ]]; then
              echo "- Verified rollback: \`${PREVIOUS_REF}\` (\`${PREVIOUS_VERSION}\`)"
            fi
          } >> "${GITHUB_STEP_SUMMARY}"
