{
  "routing": {
    "domainStrategy": "IPIfNonMatch",
    "rules": [
      {
        "inboundTag": [
          "api"
        ],
        "outboundTag": "api",
        "ruleTag": "api",
        "type": "field"
      },
      {
        "outboundTag": "blocked",
        "protocol": [
          "bittorrent"
        ],
        "ruleTag": "block-bt",
        "type": "field"
      },
      {
        "ip": [
          "geoip:private"
        ],
        "outboundTag": "blocked",
        "ruleTag": "block-private",
        "type": "field"
      },
      {
        "network": "tcp,udp",
        "outboundTag": "direct",
        "ruleTag": "default",
        "type": "field"
      }
    ]
  }
}
