# Round 6 Changelog — 2026-06-22（外部评审纠偏）

- **背景**：第二份外部 review（对已实现的阶段 1/2/3）提了 6 条，经核验**全部成立**，其中 #1/#6 是本人引入的 bug（#1 为部署阻断级）。本轮逐条修复。
- **级别**：架构（修部署阻断 + 正确性 + 防呆 + 文档）
- **分支**：`fix/monitor-integrity`

## 修复清单

| # | 问题 | 修复 |
|---|---|---|
| **1** 🔴 | monitor role `recurse:yes` chown `reality_data_dir`，会把 xray 配置(UID 10000 只读)改成 reality-monitor 0750 → **spt 上 xray 起不来** | **DB 移出共享目录**：`server.py.j2` `DB_PATH` → `{{monitor_root_dir}}/data/traffic_monitor.db`；tasks 改为只建 `monitor_root_dir/data`(reality-monitor 0750，**不 recurse、不碰 reality_data_dir**)；retention cron 路径同步；checklist 加 stop→mv→chown→start 迁移步 + xray 回归检查 |
| **2** 🔴 | `CHANGEME-*` 默认 token 可能随 vault 漏配上线 | monitor 安装块加 `assert`：report/admin/stats token 非空且 `is not match('CHANGEME')`；subs token 启用时同样挡 |
| **3** 🟡 | single 模式 `user_ip_hits.user`(reap.spt) ≠ `records.user`(reap) | `_parse_log_lines` 非 fixed_user 分支对 email 用户 `split('.')[0]` 归一（与 `get_core_stats` 一致） |
| **4** 🟡 | 读端点 `async def` 跑同步 sqlite 阻塞事件循环 | `get_ip_matrix/get_daily_stats/get_timeseries/get_health/export_stats/cleanup_stats/get_subscription_logs` 改同步 `def`（入线程池） |
| **5** 🟡 | staging 目标 guard 只在文档 | tasks 加 `assert`：`test_nodes` 节点启用监控时 `monitor.server_url` 不得含生产域名（隔离层④落地） |
| **6** 🟠 | 文档滞后 + checklist 残留 `</content>` | 删 `</content>`；README/docs/operations.md/MONITOR.md 同步 D1-B 鉴权 + 已删文件 + DB 新路径；checklist/operations DB 路径更新 + 迁移步 |

## 文件改动

- `roles/monitor/templates/server.py.j2`：DB_PATH(#1) + 7 读端点 async→def(#4)
- `roles/monitor/templates/agent.py.j2`：IP 审计用户名归一(#3)
- `roles/monitor/tasks/main.yml`：DB 专属目录非 recurse(#1) + retention 路径 + token assert(#2) + staging guard(#5)
- `README.md`、`docs/operations.md`、`MONITOR.md`(banner)、`docs/features/monitor/operations.md`(DB 路径)、`deploy-checklist`(路径/迁移/删残留)(#6)

## 3-gate

1. **方向**：✅ 对应 review 6 条。
2. **语法/静态**：✅ server/agent 渲染 `py_compile`；`tasks`+`main.yml` YAML 通过（含新 assert 结构）；checklist 无 `</content>`。⚠️ `ansible --syntax-check` + `assert` 的 `is not match` 运行语义 = 部署时验证（无 ansible）。
3. **功能**：✅ 临时实例 + 单元：
   - #1 渲染 DB_PATH = `monitor_root_dir/data/...`，无 reality_data_dir 残留；起实例 DB 实际落在 monitor/data。
   - #3 `email: reap.spt → user reap`、`cpp.spt → cpp`；multi fixed_user 不受影响。
   - #4 7 端点已无 `async def`；`/stats/daily`、`/stats/health` 带 Bearer=200（def 入线程池仍正常）、无鉴权=401；`/healthz`=200。

## 明确未做

- `ansible --syntax-check` 与 assert 运行语义、xray 不受影响、DB 迁移、CF 端到端 = **部署时验证**（见更新后的 deploy-checklist Phase 0/3）。
- MONITOR.md 仅加过时 banner + 关键纠正，未整篇重写（权威已指向 `docs/features/monitor/operations.md`）。

## Next Steps

- 评审 6 条已全部闭合，方案可进入部署前置(CF/vault)→ 金丝雀（按 deploy-checklist）。
- 部署时务必验 **#1 xray 回归**（spt 上 reality_data_dir 未被 monitor 改动、xray 容器正常）。
</content>
