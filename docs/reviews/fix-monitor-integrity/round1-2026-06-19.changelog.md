# Round 1 Changelog — 2026-06-19

- **计划**：[`plan-staging-env-2026-06-13.md`](./plan-staging-env-2026-06-13.md)（部分恢复实现）
- **级别**：架构（隔离边界）
- **分支**：`fix/monitor-integrity`

## 设计变更（相对原计划）

原计划三层隔离（① 独立 inventory ② 测试 group_vars ③ ACL 独占）。用户决定**保留生产 `inventory.ini`**、把 4 台测试机直接放进 `[reality_nodes]`，因此：

- **放弃隔离层①**（独立 `inventory.test.ini`）。隔离改靠层②③ + 运行时 `--limit`。
- 监控按用户选择**延后接入**（先验证节点可用性），不在本轮建独立测试监控。
- 层②配置放 `group_vars/test_nodes.yml`（用户负责在 inventory 加 `[test_nodes]` 组绑定）。

## 文件改动清单

| 文件 | 改动 | 意图 |
|---|---|---|
| `deploy.yml` | （已存在于工作树，本轮未改）`reality_node_users` 独占分支 + 前置校验 assert | 层③ ACL 独占：节点声明 `reality_node_users` 时只装列表内用户，绕过 group/'all' 匹配 |
| `host_vars/{hkcod12,hyu24,hyd13,hyu22}.yml` | 移除 `monitor_enabled`（交由组集中管理）；保留 `reality_mode` + `reality_node_users:[test]` | `reality_node_users` 故意留 host_vars 作 fail-safe 闸门（优先级高于 group，漏加组也不失守） |
| `group_vars/test_nodes.yml` | **新建**：`monitor_enabled:false` + `vault_github_token:""` | 层②：暂关监控；禁止测试跑推送生产 Gist |
| `inventory.ini` | 用户自改（本轮不动） | 4 台测试机入 `[reality_nodes]` + 生产档位重排（用户确认有意） |

## 关键依据

- `ansible.cfg` 无 `hash_behaviour=merge`（默认 replace）→ 覆盖 `monitor:` 须给完整字典。本轮不覆盖 monitor，故无影响；已在 `group_vars/test_nodes.yml` 注释中标注。
- `roles/monitor/tasks/main.yml:5,119` 以 `monitor_enabled|default(true)` 为开关 → `false` 跳过/清理监控代理。
- Gist 任务 `deploy.yml:126` 仅在 `ansible_play_hosts_all|first` 跑一次；`--limit` 测试机时首台为 test 节点，取空 token → 跳过。

## 明确未做（NOT done）

- **未建独立测试监控**（hkcod12 monitor / monitor-test 域名 / 独立 token）——按用户决定延后。
- **未碰 `inventory.ini`**——含其中的生产档位重排（用户有意为之）。
- **未做 §6 其余输入**（VPS 连接方式/IP、CF 子域）——监控延后后暂不需要。
- **未部署、未碰生产**。

## 自检（3-gate）

1. **方向**：符合用户两项决定（保留 inventory；监控延后、层②放 group_vars）。✅
2. **语法/静态**：4 个 host_vars + `group_vars/test_nodes.yml` `yaml.safe_load` 全部通过。✅
3. **功能**：`ansible-playbook --syntax-check` / 实跑 —— **BLOCKED**（本机未装 ansible，fresh probe 确认 `command -v ansible-playbook` 为空）。恢复部署环境后补跑。

## Next Steps

- **立即可做（用户）**：在 `inventory.ini` 加 `[test_nodes]` 组列出 4 台测试机，否则 `group_vars/test_nodes.yml` 不绑定、层②失效（层③仍靠 host_vars 生效）。
- **立即可做**：在有 ansible 的环境补 Gate 3（`ansible-playbook --syntax-check deploy.yml`；`--limit` 测试机 `--check` 试跑确认只装 test 用户、监控被跳过、Gist 被跳过）。
- **阻塞**：监控验证（§7 A1/A2/A3）依赖 plan-harden-monitor 阶段 A 代码 + 独立测试监控，均未实现。
- **可延后**：监控加固代码就绪后，翻 `group_vars/test_nodes.yml` 的 `monitor_enabled` 并补完整 monitor 字典（指向 hkcod12 + 测试域名 + 独立 vault token）。
